# GaugeGuard AI — Environment Configuration
DATABASE_URL=sqlite:///./gaugeGuard.db
BACKEND_URL=http://localhost:8000

# Optional AI explanation layer (fallback to templates if not set)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional alert channels (simulated locally if not set)
SLACK_WEBHOOK_URL=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
ALERT_EMAIL_TO=

# App settings
DEMO_MODE=true
LOG_LEVEL=INFO
